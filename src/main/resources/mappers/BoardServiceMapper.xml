<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.toyProject.dao.BoardDao">

	<select id="boardList" resultType="com.toyProject.domain.BoardVO">
		<![CDATA[
		select rownum, bno, title, writer, writedate
		from 
			(select /*+index_desc(mou_board pk_board)*/
			rownum as rn, bno, title, writer, writedate
			from mou_board where rownum <= #{maxRow})
		where rn > #{minRow}
		]]>
	</select>

	<select id="totalCount" resultType="int">
		select count(bno) as count from mou_board
	</select>

</mapper>